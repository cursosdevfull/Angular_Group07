import { ConfigLayout } from '../interfaces/config.interface';

export const AMB_Config: ConfigLayout = {
  layout: {
    header: {
      hidden: false,
    },
    menu: {
      hidden: false,
    },
  },
};
