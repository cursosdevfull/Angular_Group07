import { RolesAllowedDirective } from './roles-allowed.directive';

describe('RolesAllowedDirective', () => {
  it('should create an instance', () => {
    const directive = new RolesAllowedDirective();
    expect(directive).toBeTruthy();
  });
});
